plugins {
    id 'com.android.application'
}

android {
    namespace 'com.rovix.amigopet.cliente'
    compileSdk 35

    def baseUrl = (project.findProperty('AMIGOPET_BASE_URL') ?: 'https://amigopet-6td8.onrender.com').toString().replaceAll('/+$', '')
    def baseHost = new URI(baseUrl).host

    defaultConfig {
        applicationId 'com.rovix.amigopet.cliente'
        minSdk 23
        targetSdk 35
        versionCode 1
        versionName '1.0'
        buildConfigField 'String', 'APP_URL', "\"${baseUrl}\""
        buildConfigField 'String', 'ALLOWED_WEBVIEW_HOSTS', '""'
        manifestPlaceholders = [amigopetHost: baseHost]
    }

    buildFeatures {
        buildConfig true
    }
}

dependencies {}
