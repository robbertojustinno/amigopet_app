AMIGOPET V6 - CORREÇÃO RENDER

Arquivos corrigidos:
- backend/app/main.py
- frontend/app.js
- requirements.txt
- backend/requirements.txt
- runtime.txt
- render.yaml

Correções:
1. API do frontend deriva de location.origin ou de window.AMIGOPET_CONFIG
2. WebSocket deriva da API configurada ou de location.origin
3. requirements com passlib[bcrypt] e bcrypt
4. runtime travado em Python 3.11.9
5. Schema do banco agora ? aplicado por Alembic antes do backend iniciar.

Como aplicar:
Copie as pastas/arquivos por cima do seu projeto.
Depois rode:

git add .
git commit -m "Fix Render banco e frontend completo"
git push

No Render:
Manual Deploy -> Clear build cache & deploy
Observa??o:
- O comando de start executa `alembic upgrade head` antes do Uvicorn.
- N?o h? mais DDL improvisado no startup da aplica??o.
